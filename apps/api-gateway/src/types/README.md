# Type shims

This repo currently uses TS source entrypoints for some workspace packages.
To unblock `apps/api-gateway` compilation during the Swagger task, minimal
`declare module` shims are added here.

