{
  "name": "farm-client",
  "version": "1.0.0",
  "description": "Farm Management System - Frontend orchestrator (admin and console are independent npm projects)",
  "private": true,
  "packageManager": "npm@11.10.0",
  "scripts": {
    "build": "npm --prefix admin run build && npm --prefix console run build",
    "build:admin:full": "npm --prefix admin run build",
    "build:console:full": "npm --prefix console run build",
    "dev:admin": "npm --prefix admin run dev",
    "dev:console": "npm --prefix console run dev",
    "start:admin": "npm --prefix admin run start",
    "start:console": "npm --prefix console run start",
    "test": "npm --prefix admin run test --if-present && npm --prefix console run test --if-present",
    "lint": "npm --prefix admin run lint --if-present && npm --prefix console run lint --if-present"
  }
}
